# 知途 · 校招简历助手——会话交接文档

更新时间：2026-07-28  
项目目录：`C:\Users\lenovo\Documents\Codex\2026-07-28\ux-mvp-1-2-3-4`  
正式网站：<https://zhitu-campus-career.m72554083.chatgpt.site/>  
Sites 项目 ID：`appgprj_6a685d18afb48191a1ee93a553c72bda`

## 1. 我们正在做什么

这是一个面向应届生和实习生的“简历构建 + JD 匹配 + 求职辅助”网站，产品名为“知途 · 校招简历助手”。

最初目标是可交互 MVP，之后用户要求升级为可真实使用的版本。目前技术形态是：

- React / TypeScript / vinext（Vite 体系）
- Supabase 邮箱密码注册登录
- Supabase 云端保存每个用户的独立工作区
- 浏览器端解析 PDF、DOCX、TXT、JPG、PNG
- 浏览器端导出 PDF、PNG
- JD 分析为本地规则匹配，不接任何 AI API
- 无独立后端、无 Docker、无数据库服务器自建

产品伦理边界必须始终保持：

- 不虚构经历、项目、技能、数字或时间
- 只允许语言润色、结构调整、关键词强化和模块排序
- 所有优化建议由用户确认后才生效

## 2. 用户已经明确确认的产品决策

- 使用 Supabase。
- 使用“邮箱 + 密码”注册登录，不再使用魔法邮件链接。
- 网站对外开放。
- 不接 OpenAI、DeepSeek 或任何其他 AI API。
- JD 匹配和面试问题使用本地规则。
- 删除 DOCX 导出选项，只保留 PDF、PNG。
- PDF 可以是普通 PDF，重点是与简历预览一致。
- 简历为 A4，并支持智能一页模式。

不要擅自把上述决策改回去。

## 3. 已经完成的功能

### 账号和云端数据

- Supabase 邮箱密码注册与登录。
- 用户数据隔离。
- 云端自动保存。
- 本地旧数据兼容迁移。
- 真实历史版本保存与恢复。
- 账号退出。

Supabase 配置已经存在于本机 `.env.local` 和 Sites 正式环境中。不要把密钥硬编码进源码或交接文档。

### 简历中心

- 新建简历。
- 导入旧简历。
- 简历列表。
- 重命名、复制、删除。
- 最近编辑信息。
- 多份定向简历。

### 简历编辑器

- 基本信息。
- 多条实习经历。
- 多条教育经历。
- 多条项目经历。
- 技能、证书、自我评价、作品链接。
- 模块增删、隐藏、恢复、排序。
- 条目新增、复制、删除、上下移动。
- 实时 A4 预览。
- 模板切换。
- 智能一页模式。
- 自动保存。
- 头像上传、压缩与预览。
- 文本换行在预览中保留。

### 文件导入

- PDF 文字提取。
- DOCX 文字提取。
- TXT 解析。
- JPG、PNG 浏览器本地 OCR。
- 导入后提供字段确认，不应悄悄修改原文。

导入解析目前“能用但仍不够聪明”，是后续最值得改善的模块之一。

### JD 匹配

- 本地关键词规则解析。
- 匹配分数。
- 已匹配关键词。
- 缺失项。
- 优化建议。
- 5–8 个面试问题。
- 不发送简历内容给任何 AI 服务。

### 导出

- PDF 导出。
- PNG 导出。
- A4 页面。
- 目标是与右侧简历预览视觉一致。
- DOCX 导出已经按用户要求删除，不要恢复。

## 4. 关键代码位置

- `app/page.tsx`
  - 主应用和大部分交互。
  - 简历数据结构。
  - 云端工作区恢复。
  - 多条经历。
  - JD 规则。
  - PDF、PNG 导出。
  - 数据兼容函数 `normalizeResume`。
- `app/globals.css`
  - 全站样式、编辑器、A4 预览、登录页、错误恢复页。
- `app/error.tsx`
  - 页面渲染错误边界，避免再次显示纯空白页。
- `app/lib/supabase-client.ts`
  - 邮箱密码认证、会话刷新、工作区读取与保存。
- `app/lib/resume-import.ts`
  - PDF、DOCX、TXT、图片 OCR 和字段解析。
- `supabase/schema.sql`
  - Supabase 工作区表和 RLS 策略。
- `worker/index.ts`
  - 正式环境 Worker 入口。
  - 首页设置 `no-store`，降低旧 HTML 缓存造成的资源错配。
- `.openai/hosting.json`
  - 必须复用现有 `project_id`，绝对不要重新创建网站。
- `public/assets/`
  - 包含旧正式版本的兼容资源。不能随意删除，原因见下文。

## 5. 当前卡在哪里

### 最新状态

用户之前遇到：

1. 登录成功后显示空白页。
2. 第一次修复后，页面能显示错误恢复卡片，但仍无法进入工作台。

最终查明根因不是账号、Supabase 或简历数据，而是发布后浏览器仍引用上一正式版本的带哈希 JS/CSS 文件，而新部署包没有保留这些旧资源，导致请求 404。

已经完成的修复：

- 从上一正式版本构建目录恢复缺失资源。
- 将以下旧资源保留到 `public/assets/`：
  - `index-Db1Qk3qr.js`
  - `page-ZSAMXVp-.js`
  - `index-BIgz2ncN.css`
  - `layout-segment-context-B4I-UB8W.js`
- 重新构建并验证这些文件存在于 `dist/client/assets/`。
- 已发布正式版本 16。
- 发布后再次检查最近 5 分钟 Worker 错误日志，结果为空，不再出现资源 404。

随后用户展开错误信息，确认真正的运行时错误为：

`current is not defined`

根因是 `Dashboard` 组件中的问候语错误引用了 `Home` 组件作用域内的
`current` 变量。现已改为从传入的 `resumes` 中安全读取首个有效姓名，
并在没有姓名时显示“求职同学”。

修复已通过正式构建并发布为版本 17。

当前唯一未确认事项：

- 等待用户在原浏览器中执行 `Ctrl + F5`、点击“重新加载工作台”或关闭标签页重新打开，并确认登录后工作台正常显示。

如果用户仍然看到错误恢复页：

1. 让用户展开“查看错误信息”并截图。
2. 同时读取 Sites 最近错误日志。
3. 不要再优先猜测 Supabase 数据损坏；先检查是否又有新哈希资源 404。

## 6. 下一步计划

按优先级执行：

1. 确认用户登录后可以进入工作台。
2. 回归测试：
   - 邮箱密码登录。
   - 云端简历加载。
   - 编辑与自动保存。
   - 刷新后恢复。
   - 多条经历。
   - 头像。
   - 换行。
   - PDF、PNG 导出。
3. 改善简历导入：
   - 更可靠地区分姓名、联系方式、学校、公司、项目。
   - 支持常见中文简历标题变体。
   - 解析结果必须先让用户确认。
4. 对 PDF、PNG 做最终像素级回归，确保与预览一致。
5. 增加保存失败、网络中断和会话过期提示。
6. 做移动端基础可用性回归。
7. 清理项目根目录临时文件 `site-package-dc9b16b.tar.gz`。它只是发布包，不是产品源码；先确认精确路径后删除。

## 7. 绝对不要再踩的坑

### 不要删除 `public/assets/` 中的旧哈希资源

浏览器可能仍持有上一版本 HTML，继续请求旧文件。只发布最新哈希文件会造成登录后空白或错误页。

在部署机制彻底解决原子切换前：

- 保留最近正式版本的 JS/CSS 哈希资源。
- 构建后检查上一版本引用的资源仍存在于 `dist/client/assets/`。
- 部署后立即查看 Worker 404 日志。

### 不要只看“部署成功”

Sites 显示部署成功，只代表 Worker 发布成功，不代表浏览器引用的静态资源全部存在。

每次关键部署后至少检查：

- 根页面是否成功。
- 当前和上一版本的关键 `/assets/*.js`、`/assets/*.css` 是否返回 200。
- Worker 最近错误日志是否出现 404。

### 不要再次把空白页简单归因于缓存或云端数据

这次真正根因是资源版本错配。此前已经因为猜测缓存和旧数据而绕路。

诊断顺序应是：

1. 浏览器控制台。
2. Worker 资源 404。
3. 错误边界中的真实错误。
4. 最后才检查云端数据结构。

### 云端数据永远不能直接信任

旧版本、异常保存或手工数据可能导致类型不一致。

`normalizeResume` 和工作区恢复逻辑必须继续保证：

- 字符串字段一定为字符串。
- 数组字段一定为数组。
- 模块键只允许合法值。
- 历史版本逐条迁移。
- `currentId` 不存在时回退到首份简历。

不要删除这些容错。

### 不要恢复 AI 接入

用户明确要求砍掉 AI API。当前所谓“智能”能力是本地规则与提示。

### 不要恢复 DOCX 导出

用户明确要求删除 DOCX 导出，只保留 PDF 和 PNG。

### 不要创建新的 Sites 项目

`.openai/hosting.json` 已经有项目 ID，必须复用：

`appgprj_6a685d18afb48191a1ee93a553c72bda`

### 不要把短期发布凭证写入文件、Git 配置或远程 URL

Sites 源码写入凭证有效期很短，只能作为单次命令的 HTTP Authorization header 使用。

Windows Git 曾出现：

`schannel: AcquireCredentialsHandle failed`

可在单次推送命令中使用：

`-c http.sslBackend=openssl`

不要把 token 打印给用户，不要写入仓库。

### 发布包根目录不能打错

Sites 需要归档中存在：

- `dist/server/index.js`
- `dist/.openai/hosting.json`

归档必须保留顶层 `dist/` 目录，不能只把 `dist` 的内容放到压缩包根目录。

Windows 上运行 Sites 的 `package-site.sh` 时，绝对归档路径曾导致 `mkdir` 权限错误。有效方式是：

- 在项目目录中运行脚本。
- 给脚本传相对归档文件名。
- 发布完成后删除临时归档。

### 本地 `.git` 不能当作正常工作树使用

当前本地 `git status` 显示所有项目文件为未跟踪状态，且 `.git` 在沙箱中基本只读。

此前发布使用了临时 Git 目录，将精确源码提交并推送到 Sites 的源码仓库。不要直接做破坏性的 `git reset --hard`、`git clean` 或批量删除。

最新已推送的 Sites 源码提交：

`dc9b16be99bac48c3cb6facdde0cd2ef4a8d9693`

### 不要清空用户 Supabase 数据来“修复”页面

用户数据目前没有证据表明损坏。禁止通过清表、删除账号或覆盖为空工作区来处理前端错误。

## 8. 发布信息

最新正式版本：17  
最新部署状态：成功  
正式地址：<https://zhitu-campus-career.m72554083.chatgpt.site/>

此前关键部署：

- 版本 15：增加云端旧数据兼容和错误恢复页。
- 版本 16：补齐上一正式版本静态资源，解决登录后资源 404。
- 版本 17：修复 Dashboard 问候语引用未定义 `current` 导致的登录后崩溃。

## 9. 新会话接手后的第一句话建议

先告诉用户：

“我已读取 `HANDOFF.md`。请确认重新打开网站并登录后，现在是否能进入工作台；如果仍停在错误恢复页，请展开‘查看错误信息’并截图，我会结合线上资源日志继续定位。”

## 10. 版本 18 登录后崩溃修复

- 用户在版本 17 后仍看到 `current is not defined`。
- 根因是两个兼容保留的旧页面脚本仍包含同一错误：
  - `public/assets/page-C3PNv9eE.js`
  - `public/assets/page-ZSAMXVp-.js`
- 两个旧脚本均已改为从 `resumes` 参数安全读取姓名。
- `worker/index.ts` 已对首页和这两个旧脚本设置 `no-store`，防止继续复用错误缓存。
- 已确认 `public/` 与 `dist/` 中不再存在 `current.basic.name.trim()`。
- 正式版本 18 已成功发布。
- 如用户仍停在内存中的旧错误页，直接打开：
  `https://zhitu-campus-career.m72554083.chatgpt.site/?v=18`
  以强制获取新文档和新哈希资源；不要清空 Supabase 数据。
