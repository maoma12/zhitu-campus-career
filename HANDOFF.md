# 知途 · 校招简历助手——项目交接文档

- 更新时间：2026-07-29
- 本机项目目录：`E:\Codex\2026-07-28\ux-mvp-1-2-3-4`
- GitHub：<https://github.com/maoma12/zhitu-campus-career>
- 正式网站：<https://zhitu-campus-career.m72554083.chatgpt.site/>
- Sites 项目 ID：`appgprj_6a685d18afb48191a1ee93a553c72bda`
- 最新正式版本：Sites V20

## 1. 项目在做什么

“知途 · 校招简历助手”是一款面向应届生和实习生的简历求职辅助网站，核心流程为：

1. 注册或登录。
2. 创建或导入简历。
3. 编辑个人信息、教育、实习、项目、技能等内容。
4. 实时查看 A4 简历预览并保存历史版本。
5. 输入目标岗位 JD，进行本地规则匹配。
6. 查看匹配分数、关键词、缺失项、优化建议和面试问题。
7. 导出与预览一致的 PDF 或 PNG 简历。

项目最初是交互原型，现已推进为可公开访问、可注册登录、可云端保存的真实可用版本。

## 2. 不得改变的产品边界

- 不接 OpenAI、DeepSeek 或其他 AI API。
- “智能匹配”和提示均使用浏览器端本地规则。
- 不虚构用户的经历、项目、技能、数字或时间。
- 只允许润色、结构调整、关键词强化和模块排序。
- 优化建议必须由用户主动确认，不能偷偷修改简历。
- 账号使用 Supabase“邮箱 + 密码”注册登录，不使用魔法邮件链接。
- 不恢复 DOCX 导出；当前只保留 PDF 和 PNG 导出。
- 网站继续复用现有 Sites 项目，不得创建新的 Sites 项目。
- 不得清空、覆盖或重建用户的 Supabase 数据来解决前端问题。

## 3. 当前技术架构

- 前端：React 19、TypeScript、vinext、Vite
- 样式：全局 CSS，现代 SaaS 三栏编辑器布局
- 身份认证：Supabase Auth 邮箱密码注册登录
- 数据：Supabase 云端工作区，按用户隔离；兼容旧版 LocalStorage 数据迁移
- 简历导入：浏览器端解析 PDF、DOCX、TXT、JPG、PNG
- 图片文字识别：浏览器本地 OCR
- JD 匹配：本地关键词规则
- 导出：浏览器端 PDF、PNG
- 部署：OpenAI Sites
- 代码托管：GitHub
- 无独立业务后端、无 Docker、无自建数据库服务、无 AI API

## 4. 已经完成的功能

### 4.1 账号与数据

- 邮箱密码注册、登录、退出。
- 用户数据隔离和云端自动保存。
- 云端工作区读取、恢复和刷新后继续编辑。
- 本地旧数据兼容迁移。
- 真实历史版本保存与恢复。
- 对旧版或异常数据执行类型归一化，避免页面崩溃。

### 4.2 简历中心

- 新建、重命名、复制、删除简历。
- 多份定向简历管理。
- 最近编辑时间和版本信息。
- 导入旧简历。

### 4.3 简历编辑器

- 基本信息、教育经历、实习经历、项目经历。
- 技能特长、证书荣誉、自我评价、作品展示。
- 多条经历新增、复制、删除和上下移动。
- 标准模块自定义名称。
- 自定义简历模块新增、编辑、隐藏和恢复。
- 模块排序。
- 头像上传、压缩、更换和预览。
- 文本换行在右侧预览中保留。
- 选中文本后使用加粗按钮，以 `**文本**` 保存并在预览、PNG 中渲染粗体。
- 模板切换。
- 自动保存。

### 4.4 A4 预览与分页

- 右侧 A4 实时预览。
- 智能一页模式。
- 内容超出一页后自动增加页面。
- 分页时保留前一页底部和后一页顶部留白。
- 尽量避免标题单独落在前一页底部。
- 内容减少后重新计算页数并自动删除空白页。
- 预览与打印共用 610px 内容布局，打印时缩放到精确 A4。
- 页面之间显示清晰的分页间隔。

### 4.5 文件导入

- PDF 文字提取。
- DOCX 文字提取。
- TXT 解析。
- JPG、PNG 浏览器本地 OCR。
- 导入后提供字段确认，不应静默篡改原文。

当前导入功能可以使用，但对复杂双栏简历、图文混排和非标准标题的识别仍需要提升。

### 4.6 JD 本地匹配

- JD 文本输入。
- 关键词识别。
- 简历匹配分数。
- 已匹配关键词和缺失项。
- 基于现有内容的优化建议。
- 生成 5–8 个规则型面试问题。
- 简历正文不会发送给 AI 服务。

### 4.7 导出

- PDF 导出。
- PNG 导出。
- DOCX 导出已删除。
- PDF 打印时只克隆简历预览到独立的 `#resume-print-root`，避免原页面与打印副本重复输出两份。
- PDF、PNG 以 A4 预览为视觉基准。
- 支持多页内容。

## 5. 关键代码位置

- `app/page.tsx`
  - 主应用、页面路由状态和核心交互。
  - 简历数据结构与兼容函数。
  - 多条经历和自定义模块。
  - A4 预览、分页、加粗渲染。
  - PDF、PNG 导出。
  - JD 本地规则。
- `app/globals.css`
  - 全站布局和视觉样式。
  - A4 预览、分页间隔、打印样式。
  - 登录页和错误恢复页样式。
- `app/error.tsx`
  - 页面错误边界，防止运行时错误只显示空白页。
- `app/lib/supabase-client.ts`
  - Supabase 认证、会话和工作区读写。
- `app/lib/resume-import.ts`
  - PDF、DOCX、TXT、图片 OCR 和字段解析。
- `supabase/schema.sql`
  - Supabase 数据表和 RLS 用户隔离策略。
- `worker/index.ts`
  - 正式环境 Worker 入口与缓存控制。
- `.openai/hosting.json`
  - 保存现有 Sites `project_id`，移动项目时必须一并保留。
- `public/assets/`
  - 保留过渡期旧哈希资源，避免旧 HTML 请求旧资源时出现 404。

## 6. 当前状态与卡点

### 当前状态

- 本地生产构建通过。
- GitHub 已更新。
- Sites V20 已成功发布。
- 正式网站可访问。
- 当前没有已知阻塞性故障。

### 当前非阻塞问题

1. 简历导入对复杂排版的解析准确率仍不够高。
2. 需要继续对不同内容长度进行 PDF、PNG 多页回归测试。
3. 需要完善网络中断、云端保存失败和会话过期提示。
4. 需要做手机端基础可用性适配和测试。
5. 需要补充隐私政策、服务条款、账号注销和数据删除流程，才能更稳妥地向公众正式推广。
6. 需要配置正式邮件发送服务、监控告警、错误日志和数据备份策略。

## 7. 下一步计划

按优先级推进：

1. 回归测试注册、登录、退出、会话过期和多账号数据隔离。
2. 使用短内容、恰好一页、略超一页、两页以上内容测试动态分页。
3. 对 PDF 和 PNG 与右侧预览进行像素级对比。
4. 改进导入解析：
   - 支持常见中文模块标题变体。
   - 更可靠地区分姓名、学校、公司、职位和项目。
   - 增加字段确认和未识别内容区域。
5. 增加保存状态、失败重试、离线提示和冲突提示。
6. 补充账号注销、云端数据删除、隐私政策和服务条款。
7. 增加基础监控、错误上报、访问统计和 Supabase 备份。
8. 完成桌面主流浏览器与移动端兼容测试。

## 8. 绝对不要再踩的坑

### 8.1 不要恢复 AI 或 DOCX 导出

这是用户明确确认的产品决定。除非用户以后再次明确要求，否则不得改回。

### 8.2 不要创建新的 Sites 项目

必须复用：

`appgprj_6a685d18afb48191a1ee93a553c72bda`

移动本机项目时，务必保留 `.openai/hosting.json`。

### 8.3 不要清空 Supabase 数据解决页面错误

此前的登录后空白页不是数据损坏，而是静态资源版本错配和前端变量作用域错误。应优先检查浏览器控制台、错误边界和 Worker 资源日志。

### 8.4 不要删除 `public/assets/` 中的旧哈希资源

旧页面缓存可能继续请求上一版本的 JS/CSS。过早删除旧资源会导致登录后空白页或资源 404。

部署机制彻底实现原子切换前，应：

- 保留最近正式版本的关键哈希资源。
- 构建后确认旧资源仍存在。
- 部署后检查 `/assets/*.js` 和 `/assets/*.css`。

### 8.5 不要只看“部署成功”

Sites 成功只代表生产 Worker 已发布，不代表浏览器端全部正常。关键发布后还需检查：

- 首页和登录页。
- 登录后工作台。
- 当前及上一版本静态资源。
- 编辑、保存、刷新恢复。
- PDF、PNG 导出。

### 8.6 云端数据必须归一化

不要删除 `normalizeResume` 及相关兼容逻辑。读取云端或历史数据时必须保证：

- 字符串字段为字符串。
- 数组字段为数组。
- 模块键合法。
- 自定义模块结构完整。
- `currentId` 无效时回退到首份简历。

### 8.7 PDF 不要直接打印整个应用页面

直接打印页面会把原预览和克隆副本一起输出，曾造成重复两页。只能打印独立的 `#resume-print-root`。

### 8.8 分页计算不要把分页辅助元素当成正文

页数必须基于 `[data-pagination-block]` 正文块重新计算。分页线、页间距和上一轮分页占位不能参与高度统计，否则内容减少后会残留空白页。

### 8.9 发布包结构不能错误

Sites 归档必须包含：

- `dist/server/index.js`
- `dist/.openai/hosting.json`

归档顶层必须保留 `dist/`。

### 8.10 Windows Git 推送 Sites 时要使用 OpenSSL

Windows Git 曾出现：

`schannel: AcquireCredentialsHandle failed`

Sites 短期凭证只能用于单次命令的 HTTP Authorization header，并加：

`-c http.sslBackend=openssl`

不得把 token 写入文件、Git 配置、远程 URL、日志或交接文档。

### 8.11 当前本地 `.git` 不能视为可靠的同步仓库

当前本地 `.git` 在运行环境中近似只读，`git status` 会把大量文件显示为未跟踪。此前 GitHub 更新使用 GitHub 连接器，Sites 更新使用临时 Git 工作目录。

禁止执行：

- `git reset --hard`
- `git clean -fd`
- 对项目根目录做批量删除

## 9. 最新版本和提交

### GitHub

仓库：<https://github.com/maoma12/zhitu-campus-career>

本次 V20 对应的 GitHub 更新：

- `aa7c55fc9dc809563154ebeb5e7ee10ba50f4580`
  - 修复简历分页和 A4 导出。
- `506abfb643fc8df35500fdd5e8368e711b6757da`
  - 优化分页视觉和加粗功能。

GitHub 使用提交历史保存新版本，不是不可恢复的覆盖。旧提交仍可查看和回退。

### Sites

- 最新正式版本：V20
- Sites 源码提交：`609e4c8a7a084f5a5929d8263f87947b22f14ed0`
- 正式网址：<https://zhitu-campus-career.m72554083.chatgpt.site/>
- 部署状态：成功

## 10. 本机文件与迁移说明

当前完整项目位于：

`C:\Users\lenovo\Documents\Codex\2026-07-28\ux-mvp-1-2-3-4`

项目可以移动到其他位置。安全做法：

1. 先复制整个文件夹到新位置，不要立即删除旧目录。
2. 必须包含隐藏目录和文件，尤其是：
   - `.openai/hosting.json`
   - `.env.local`
   - `.gitignore`
   - `public/assets/`
3. `node_modules`、`dist`、临时发布压缩包和开发日志可以不复制，之后重新执行 `npm install` 和 `npm run build`。
4. 在新目录执行构建并确认网站可运行。
5. 更新 Codex 工作目录，后续只在新目录维护。
6. 验证成功后再处理旧目录。

`.env.local` 含环境配置，不要上传 GitHub，也不要把内容复制到聊天或交接文档。

## 11. 新会话接手建议

新会话应先阅读本文件，然后：

1. 确认用户当前要修改的是本地、GitHub 还是正式站。
2. 修改前检查 `app/page.tsx`、`app/globals.css` 和 `.openai/hosting.json`。
3. 构建验证后先让用户测试；用户确认后再更新 GitHub 和正式站。
4. 不恢复 AI、DOCX，不清空 Supabase，不创建新 Sites 项目。
